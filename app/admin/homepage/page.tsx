import Link from'next/link';import{HomepageManager}from'@/components/homepage-manager';import{requireAdmin}from'@/lib/auth';import{createAdminClient}from'@/lib/supabase/admin';export default async function Page(){await requireAdmin();const client=createAdminClient();if(!client)return null;const{data}=await client.from('homepage_sections').select('id,section_type,title,is_enabled,sort_order').order('sort_order');return <div className="admin-page"><div className="admin-title"><div><span>STOREFRONT CMS</span><h1>Homepage builder</h1><p>Add, reorder and publish sections; slider artwork lives in Sliders & banners.</p></div><Link href="/" className="text-link">View storefront →</Link></div><HomepageManager initial={data??[]}/></div>}
