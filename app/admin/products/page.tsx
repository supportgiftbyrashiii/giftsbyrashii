import Link from'next/link';import{AdminProductsTable}from'@/components/admin-products-table';import{requireAdmin}from'@/lib/auth';import{createAdminClient}from'@/lib/supabase/admin';export default async function Page(){await requireAdmin();const client=createAdminClient();if(!client)return null;const{data}=await client.from('products').select('id,name,sku,price,stock,is_active,publication_status,updated_at').order('updated_at',{ascending:false});return <div className="admin-page"><div className="admin-title"><div><span>CATALOG</span><h1>Products</h1><p>Select, publish, archive or safely delete products from one place.</p></div><Link href="/admin/products/new" className="button button-primary">+ Add product</Link></div><AdminProductsTable products={(data??[]).map(product=>({...product,price:Number(product.price)}))}/></div>}
